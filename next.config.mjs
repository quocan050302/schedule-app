/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["lh3.googleusercontent.com", "scontent-syd2-1.xx.fbcdn.net"],
  },
};

export default nextConfig;
